AWSTemplateFormatVersion: "2010-09-09"

Resources:
  S3Bucket:
    Type: "AWS::S3::Bucket"
    Properties:
      BucketName: dd
      Tags:
      - Key: Name
        Value: !Sub "${AWS::StackName}-bucket"